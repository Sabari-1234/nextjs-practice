export const metadata: Metadata = {
  //for template
  //title: "blog",

  //without template

  title: {
    absolute: "blog",
  },
};

import { Metadata } from "next";
import React from "react";

const page = () => {
  return <h1>block routes</h1>;
};

export default page;
